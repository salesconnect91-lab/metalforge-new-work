import {Routes,Route} from "react-router-dom";import WarehouseStock from "../godown/WarehouseStock";import StockMovements from "../godown/StockMovements";import {useCallback,useEffect,useState} from "react";import {supabase} from "@/lib/supabase";import {toUrduName} from "@/lib/urdu";import DataTable,{Column} from "@/components/DataTable";import {Modal,ErrorBanner,ConfirmModal} from "@/components/ui";import {Printer,FileSpreadsheet,Building2,Plus} from "lucide-react";
export default function Godown(){return <Routes><Route path="/" element={<WarehouseStock/>}/><Route path="/movements" element={<StockMovements/>}/><Route path="/aging" element={<StockAgingReport/>}/><Route path="/master" element={<GodownsMasterPage/>}/></Routes>}
type WarehouseRow={id:string;name:string;name_urdu:string|null;location:string|null};type GodownRow={id:string;name:string;name_urdu:string|null;location:string|null;warehouse_id:string|null;warehouse?:WarehouseRow|null;created_at:string};
function GodownsMasterPage(){const[rows,setRows]=useState<GodownRow[]>([]),[warehouses,setWarehouses]=useState<WarehouseRow[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null),[modalOpen,setModalOpen]=useState(false),[editing,setEditing]=useState<GodownRow|null>(null),[deleteId,setDeleteId]=useState<string|null>(null),[saving,setSaving]=useState(false),[form,setForm]=useState({name:"",name_urdu:"",location:"",warehouse_id:""});const fetchGodowns=useCallback(async()=>{setLoading(true);const[g,w]=await Promise.all([supabase.from("godowns").select("id,name,name_urdu,location,warehouse_id,created_at").order("name"),supabase.from("warehouses").select("id,name,name_urdu,location").order("name")]);if(g.error||w.error){setError(g.error?.message||w.error?.message||"Load failed");setLoading(false);return}const wr=(w.data??[]) as WarehouseRow[],map=new Map(wr.map(x=>[x.id,x]));setWarehouses(wr);setRows(((g.data??[]) as GodownRow[]).map(x=>({...x,warehouse:x.warehouse_id?map.get(x.warehouse_id)??null:null})));setLoading(false)},[]);useEffect(()=>{void fetchGodowns()},[fetchGodowns]);const reset=()=>setForm({name:"",name_urdu:"",location:"",warehouse_id:""});const save=async(e:React.FormEvent)=>{e.preventDefault();if(!form.name.trim()||!form.warehouse_id)return setError("Godown name and warehouse are required.");setSaving(true);const p={name:form.name.trim(),name_urdu:form.name_urdu.trim()||toUrduName(form.name),location:form.location.trim()||null,warehouse_id:form.warehouse_id};const q=editing?await supabase.from("godowns").update(p).eq("id",editing.id):await supabase.from("godowns").insert(p);setSaving(false);if(q.error)return setError(q.error.message);setModalOpen(false);setEditing(null);reset();await fetchGodowns()};const del=async()=>{if(!deleteId)return;const{error}=await supabase.from("godowns").delete().eq("id",deleteId);if(error)setError(error.message);else{setDeleteId(null);await fetchGodowns()}};const cols:Column<GodownRow>[]=[{key:"name",label:"Godown / گودام",render:r=><div><div className="flex items-center gap-2 font-medium"><Building2 className="h-4 w-4"/>{r.name}</div><div className="text-right text-sm text-slate-500" dir="rtl">{r.name_urdu||"—"}</div></div>},{key:"warehouse",label:"Warehouse / ویئرہاؤس",render:r=><div><div>{r.warehouse?.name||"—"}</div><div dir="rtl" className="text-right text-xs text-slate-500">{r.warehouse?.name_urdu||""}</div></div>},{key:"location",label:"Location",render:r=>r.location||"—"},{key:"created_at",label:"Added",render:r=>new Date(r.created_at).toLocaleDateString()},{key:"actions",label:"",render:r=><div className="text-right"><button className="mr-3 text-primary-600" onClick={()=>{setEditing(r);setForm({name:r.name,name_urdu:r.name_urdu??toUrduName(r.name),location:r.location??"",warehouse_id:r.warehouse_id??""});setModalOpen(true)}}>Edit</button><button className="text-red-600" onClick={()=>setDeleteId(r.id)}>Delete</button></div>}];return <div className="space-y-5"><div className="flex justify-between rounded-xl border bg-white p-5"><div><h1 className="text-2xl font-bold">Godowns Master / گودام ماسٹر</h1><p className="text-sm text-slate-500">Automatic editable Urdu names</p></div><button className="btn-primary" onClick={()=>{setEditing(null);reset();setModalOpen(true)}}><Plus className="h-4 w-4"/> Add Godown</button></div>{error&&<ErrorBanner message={error}/>}<DataTable columns={cols} rows={rows} loading={loading}/><Modal open={modalOpen} title={editing?"Edit Godown":"Add Godown"} onClose={()=>setModalOpen(false)}><form onSubmit={save} className="space-y-4"><div><label className="label">Warehouse</label><select className="input" value={form.warehouse_id} onChange={e=>setForm(x=>({...x,warehouse_id:e.target.value}))}><option value="">Select Warehouse</option>{warehouses.map(w=><option key={w.id} value={w.id}>{w.name}{w.name_urdu?` / ${w.name_urdu}`:""}</option>)}</select></div><div><label className="label">Godown Name (English)</label><input className="input" value={form.name} onChange={e=>setForm(x=>({...x,name:e.target.value,name_urdu:(!x.name_urdu||x.name_urdu===toUrduName(x.name))?toUrduName(e.target.value):x.name_urdu}))}/></div><div><label className="label">Urdu Name / اردو نام</label><input dir="rtl" className="input text-right" value={form.name_urdu} onChange={e=>setForm(x=>({...x,name_urdu:e.target.value}))}/></div><div><label className="label">Location</label><input className="input" value={form.location} onChange={e=>setForm(x=>({...x,location:e.target.value}))}/></div><div className="flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={()=>setModalOpen(false)}>Cancel</button><button className="btn-primary" disabled={saving}>{saving?"Saving...":"Save"}</button></div></form></Modal><ConfirmModal open={!!deleteId} title="Delete Godown" message="Are you sure?" onConfirm={del} onCancel={()=>setDeleteId(null)}/></div>}
type AgingRow={id:string;item_name:string;item_name_urdu:string;sku:string;godown:string;quantity:number;updated_at:string;aging_days:number};function StockAgingReport(){const[rows,setRows]=useState<AgingRow[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const{data}=await supabase.from("warehouse_stock").select("*, item:items(*)").order("updated_at");if(data){const now=Date.now();setRows(data.map((r:any)=>({id:r.id,item_name:r.item?.name??"—",item_name_urdu:r.item?.name_urdu??"",sku:r.item?.sku??"—",godown:r.godown,quantity:r.quantity,updated_at:r.updated_at,aging_days:Math.max(0,Math.floor((now-new Date(r.updated_at||r.created_at).getTime())/86400000))})))}setLoading(false)})()},[]);const total=rows.reduce((s,r)=>s+r.quantity,0);const exportCsv=()=>{let c="data:text/csv;charset=utf-8,Item Name,Urdu Name,SKU,Godown,Quantity,Aging Days\r\n";rows.forEach(r=>c+=`"${r.item_name}","${r.item_name_urdu}","${r.sku}","${r.godown}",${r.quantity},${r.aging_days}\r\n`);const a=document.createElement("a");a.href=encodeURI(c);a.download="Stock_Aging_Report.csv";a.click()};const cols:Column<AgingRow>[]=[{key:"item_name",label:"Item",render:r=><div><div>{r.item_name}</div><div dir="rtl" className="text-right text-xs text-slate-500">{r.item_name_urdu}</div></div>},{key:"sku",label:"SKU"},{key:"godown",label:"Godown"},{key:"quantity",label:"Quantity",render:r=>r.quantity.toLocaleString()},{key:"aging_days",label:"Aging",render:r=>`${r.aging_days} Days`}];return <div className="space-y-5"><div className="flex justify-between rounded-xl border bg-white p-5"><div><h1 className="text-2xl font-bold">Stock Aging / اسٹاک ایجنگ</h1><p>Total Stock: {total.toLocaleString()} kg</p></div><div className="flex gap-2"><button className="btn-secondary" onClick={exportCsv}><FileSpreadsheet className="h-4 w-4"/>Export</button><button className="btn-secondary" onClick={()=>window.print()}><Printer className="h-4 w-4"/>Print</button></div></div><DataTable columns={cols} rows={rows} loading={loading}/></div>}