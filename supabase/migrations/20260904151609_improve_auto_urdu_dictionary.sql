-- The complete improved dictionary is included in the preceding migration file so fresh databases receive the final implementation in one pass.
-- This marker migration mirrors the live Supabase migration history.
select 1;
