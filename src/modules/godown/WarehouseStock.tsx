import { useEffect, useMemo, useState } from "react";
import { Search, Package, Warehouse as WarehouseIcon, RefreshCw, SlidersHorizontal, X, Save, FileSpreadsheet, Printer } from "lucide-react";
import * as XLSX from "xlsx";
import { supabase } from "@/lib/supabase";

type Item={id:string;sku:string;name:string;name_urdu:string|null;grade:string|null;size:string|null;unit:string|null};
type Warehouse={id:string;name:string;name_urdu:string|null;location:string|null};
type Godown={id:string;name:string;name_urdu:string|null;location:string|null;warehouse_id:string};
type StockRow={id:string;user_id:string;item_id:string|null;warehouse_id:string|null;godown_id:string|null;godown:string;quantity:number;updated_at:string;item:Item|null};
type AdjustmentForm={item_id:string;warehouse_id:string;godown_id:string;qty:string;reference:string};

const EMPTY_FORM:AdjustmentForm={item_id:"",warehouse_id:"",godown_id:"",qty:"",reference:""};
const clean=(v:unknown)=>String(v??"").trim();
const esc=(v:unknown)=>clean(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]||c));
const bilingual=(en:string,ur?:string|null)=>ur?.trim()?`${en} / ${ur.trim()}`:en;
const qtyText=(qty:number,unit?:string|null)=>`${Number(qty||0).toLocaleString(undefined,{maximumFractionDigits:3})} ${clean(unit)||"UOM"}`;
const errText=(x:unknown)=>typeof x==="object"&&x!==null&&"message" in x?String((x as {message?:unknown}).message||"Unknown error"):x instanceof Error?x.message:String(x||"Unknown error");

export default function WarehouseStock(){
 const[items,setItems]=useState<Item[]>([]),[warehouses,setWarehouses]=useState<Warehouse[]>([]),[godowns,setGodowns]=useState<Godown[]>([]),[stock,setStock]=useState<StockRow[]>([]);
 const[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[search,setSearch]=useState(""),[warehouseFilter,setWarehouseFilter]=useState("all"),[godownFilter,setGodownFilter]=useState("all"),[itemFilter,setItemFilter]=useState("all"),[modalOpen,setModalOpen]=useState(false),[form,setForm]=useState<AdjustmentForm>(EMPTY_FORM);
 const[message,setMessage]=useState<{type:"success"|"error";text:string}|null>(null);
 const notify=(type:"success"|"error",text:string)=>{setMessage({type,text});window.setTimeout(()=>setMessage(null),5000)};

 const fetchData=async()=>{setLoading(true);const[itemsRes,warehousesRes,godownsRes,stockRes]=await Promise.all([
  supabase.from("items").select("id,sku,name,name_urdu,grade,size,unit").order("name"),
  supabase.from("warehouses").select("id,name,name_urdu,location").order("name"),
  supabase.from("godowns").select("id,name,name_urdu,location,warehouse_id").order("name"),
  supabase.from("warehouse_stock").select("id,user_id,item_id,warehouse_id,godown_id,godown,quantity,updated_at").order("updated_at",{ascending:false})]);
  if(itemsRes.error){notify("error",`Items load failed: ${itemsRes.error.message}`);setItems([])}else setItems((itemsRes.data??[]) as Item[]);
  if(warehousesRes.error){notify("error",`Warehouses load failed: ${warehousesRes.error.message}`);setWarehouses([])}else setWarehouses((warehousesRes.data??[]) as Warehouse[]);
  if(godownsRes.error){notify("error",`Godowns load failed: ${godownsRes.error.message}`);setGodowns([])}else setGodowns((godownsRes.data??[]) as Godown[]);
  if(stockRes.error){notify("error",`Stock load failed: ${stockRes.error.message}`);setStock([])}else{const itemMap=new Map((itemsRes.data??[]).map((i:any)=>[i.id,i]));setStock(((stockRes.data??[]) as any[]).map(r=>({...r,item:itemMap.get(r.item_id)??null})) as StockRow[])}
  setLoading(false)};
 useEffect(()=>{void fetchData()},[]);

 const filteredGodownsForFilter=useMemo(()=>warehouseFilter==="all"?godowns:godowns.filter(g=>g.warehouse_id===warehouseFilter),[godowns,warehouseFilter]);
 const modalGodowns=useMemo(()=>form.warehouse_id?godowns.filter(g=>g.warehouse_id===form.warehouse_id):[],[godowns,form.warehouse_id]);
 const warehouse=(id:string|null)=>warehouses.find(w=>w.id===id);const godown=(id:string|null)=>godowns.find(g=>g.id===id);
 const warehouseName=(id:string|null)=>{const w=warehouse(id);return w?bilingual(w.name,w.name_urdu):"—"};
 const godownName=(id:string|null,fallback?:string)=>{const g=godown(id);return g?bilingual(g.name,g.name_urdu):(fallback??"—")};
 const filteredStock=useMemo(()=>{const q=search.trim().toLowerCase();return stock.filter(row=>{const item=row.item;const searchable=`${item?.sku??""} ${item?.name??""} ${item?.name_urdu??""} ${item?.grade??""} ${item?.size??""} ${item?.unit??""} ${warehouseName(row.warehouse_id)} ${godownName(row.godown_id,row.godown)}`.toLowerCase();return(!q||searchable.includes(q))&&(warehouseFilter==="all"||row.warehouse_id===warehouseFilter)&&(godownFilter==="all"||row.godown_id===godownFilter)&&(itemFilter==="all"||row.item_id===itemFilter)})},[stock,search,warehouseFilter,godownFilter,itemFilter,warehouses,godowns]);
 const totalPositiveItems=useMemo(()=>filteredStock.filter(r=>Number(r.quantity)>0).length,[filteredStock]);
 const quantitySummary=useMemo(()=>{const units=new Set(filteredStock.map(r=>clean(r.item?.unit)||"UOM"));if(!filteredStock.length)return"0";if(units.size!==1)return"Mixed UOM / مختلف اکائیاں";const unit=[...units][0];return qtyText(filteredStock.reduce((s,r)=>s+Number(r.quantity||0),0),unit)},[filteredStock]);

 const openAdjustment=()=>{const warehouseId=warehouses[0]?.id??"";const firstGodown=godowns.find(g=>g.warehouse_id===warehouseId)?.id??"";setForm({...EMPTY_FORM,warehouse_id:warehouseId,godown_id:firstGodown});setModalOpen(true)};
 const closeModal=()=>{if(saving)return;setModalOpen(false);setForm(EMPTY_FORM)};
 const saveAdjustment=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!form.item_id){notify("error","Select an item / آئٹم منتخب کریں۔");return}if(!form.warehouse_id){notify("error","Select a warehouse / ویئرہاؤس منتخب کریں۔");return}if(!form.godown_id){notify("error","Select a godown / گودام منتخب کریں۔");return}if(!form.reference.trim()){notify("error","Adjustment reason/reference is required.");return}const selectedGodown=godowns.find(g=>g.id===form.godown_id);if(!selectedGodown||selectedGodown.warehouse_id!==form.warehouse_id){notify("error","Selected godown does not belong to selected warehouse.");return}const qty=Number(form.qty);if(!Number.isFinite(qty)||qty<0){notify("error","New physical quantity cannot be negative.");return}setSaving(true);try{const{error}=await supabase.rpc("apply_stock_movement",{p_item_id:form.item_id,p_warehouse_id:form.warehouse_id,p_godown_id:form.godown_id,p_type:"adjust",p_qty:Number(qty.toFixed(3)),p_reference:form.reference.trim()});if(error)throw error;notify("success",`Stock adjustment saved. New quantity: ${qtyText(qty,items.find(i=>i.id===form.item_id)?.unit)}.`);setModalOpen(false);setForm(EMPTY_FORM);await fetchData()}catch(x){notify("error",`Stock update failed: ${errText(x)}`)}finally{setSaving(false)}};

 const exportExcel=()=>{if(!filteredStock.length){notify("error","No stock to export.");return}const rows=filteredStock.map(r=>({Warehouse:warehouseName(r.warehouse_id),Godown:godownName(r.godown_id,r.godown),SKU:r.item?.sku??"Unknown","Item Name":r.item?bilingual(r.item.name,r.item.name_urdu):"Unknown Item",Grade:r.item?.grade??"",Size:r.item?.size??"","Quantity / UOM":qtyText(Number(r.quantity||0),r.item?.unit),Quantity:Number(r.quantity??0),UOM:r.item?.unit??"","Last Updated":r.updated_at}));const ws=XLSX.utils.json_to_sheet(rows),wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,"Warehouse Stock");XLSX.writeFile(wb,"warehouse_stock.xlsx");notify("success","Stock exported successfully.")};
 const printPdf=()=>{const rows=filteredStock.map((r,i)=>`<tr><td>${i+1}</td><td>${esc(warehouseName(r.warehouse_id))}</td><td>${esc(godownName(r.godown_id,r.godown))}</td><td><b>${esc(r.item?.sku??"—")}</b><br>${esc(r.item?bilingual(r.item.name,r.item.name_urdu):"Unknown Item")}</td><td>${esc(r.item?.grade||"—")}</td><td>${esc(r.item?.size||"—")}</td><td class="num">${esc(qtyText(Number(r.quantity||0),r.item?.unit))}</td><td>${esc(new Date(r.updated_at).toLocaleString())}</td></tr>`).join("");const w=window.open("","_blank","width=1280,height=850");if(!w){notify("error","Allow pop-ups to open print/PDF preview.");return}w.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>Current Stock - NAVILO</title><style>@page{size:A4 landscape;margin:10mm}body{font-family:Arial,sans-serif;color:#172033;font-size:9pt}h1{margin:0 0 4mm}.meta{margin-bottom:4mm;color:#64748b}table{width:100%;border-collapse:collapse;table-layout:fixed}thead{display:table-header-group}tr{break-inside:avoid}th{background:#e9eef5;border:1px solid #bfc9d6;padding:2mm;text-align:left}td{border:1px solid #d8e0e9;padding:2mm;vertical-align:top;overflow-wrap:anywhere}.num{text-align:right;font-weight:700}</style></head><body><div><b>NAVILO ERP</b></div><h1>Current Stock / موجودہ اسٹاک</h1><div class="meta">Generated: ${esc(new Date().toLocaleString())} • Records: ${filteredStock.length} • Total: ${esc(quantitySummary)}</div><table><thead><tr><th>#</th><th>Warehouse</th><th>Godown</th><th>Item</th><th>Grade</th><th>Size</th><th>Quantity / UOM</th><th>Updated</th></tr></thead><tbody>${rows||'<tr><td colspan="8">No stock records found.</td></tr>'}</tbody></table><script>window.onload=()=>setTimeout(()=>window.print(),180)<\/script></body></html>`);w.document.close()};

 const selectedItem=items.find(i=>i.id===form.item_id);const uom=clean(selectedItem?.unit)||"UOM";
 return <div className="space-y-5">
  {message&&<div className={`fixed right-5 top-5 z-[100] max-w-lg rounded-xl border px-4 py-3 text-sm font-medium shadow-xl ${message.type==="success"?"border-emerald-200 bg-emerald-50 text-emerald-800":"border-rose-200 bg-rose-50 text-rose-800"}`}>{message.text}</div>}
  <div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="flex items-start gap-3"><div className="rounded-xl bg-slate-100 p-3"><Package className="h-6 w-6 text-slate-700"/></div><div><h1 className="text-xl font-bold text-slate-900">Warehouse Stock / گودام اسٹاک</h1><p className="mt-1 text-sm text-slate-500">Purchase, Sales, Returns, Production and Transfers create their own stock movements. Manual changes are controlled adjustments only.</p></div></div><button type="button" onClick={openAdjustment} className="inline-flex items-center gap-2 rounded-xl bg-primary-600 px-4 py-2.5 text-sm font-bold text-white"><SlidersHorizontal className="h-4 w-4"/>Stock Adjustment / اسٹاک ایڈجسٹمنٹ</button></div>
  <div className="grid grid-cols-1 gap-4 md:grid-cols-3"><div className="rounded-2xl border bg-white p-4"><p className="text-xs font-semibold uppercase text-slate-400">Stock Records</p><p className="mt-1 text-2xl font-bold">{filteredStock.length}</p></div><div className="rounded-2xl border bg-white p-4"><p className="text-xs font-semibold uppercase text-slate-400">Positive Stock</p><p className="mt-1 text-2xl font-bold">{totalPositiveItems}</p></div><div className="rounded-2xl border bg-white p-4"><p className="text-xs font-semibold uppercase text-slate-400">Total Quantity / UOM</p><p className="mt-1 text-2xl font-bold">{quantitySummary}</p></div></div>
  <div className="rounded-2xl border bg-white p-4"><div className="grid grid-cols-1 gap-3 md:grid-cols-2 xl:grid-cols-4"><div className="flex items-center gap-2 rounded-xl border bg-slate-50 px-3 py-2.5"><Search className="h-4 w-4 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search item, SKU..." className="w-full bg-transparent text-sm outline-none"/></div><select value={warehouseFilter} onChange={e=>{setWarehouseFilter(e.target.value);setGodownFilter("all")}} className="rounded-xl border px-3 py-2.5"><option value="all">All Warehouses</option>{warehouses.map(w=><option key={w.id} value={w.id}>{bilingual(w.name,w.name_urdu)}</option>)}</select><select value={godownFilter} onChange={e=>setGodownFilter(e.target.value)} className="rounded-xl border px-3 py-2.5"><option value="all">All Godowns</option>{filteredGodownsForFilter.map(g=><option key={g.id} value={g.id}>{bilingual(g.name,g.name_urdu)}</option>)}</select><select value={itemFilter} onChange={e=>setItemFilter(e.target.value)} className="rounded-xl border px-3 py-2.5"><option value="all">All Items</option>{items.map(i=><option key={i.id} value={i.id}>{i.sku} — {bilingual(i.name,i.name_urdu)} — {clean(i.unit)||"—"}</option>)}</select></div><div className="mt-3 flex justify-end gap-2"><button type="button" onClick={()=>void fetchData()} className="btn-secondary"><RefreshCw className="h-4 w-4"/>Refresh</button><button type="button" onClick={exportExcel} className="btn-secondary"><FileSpreadsheet className="h-4 w-4"/>Export Excel</button><button type="button" onClick={printPdf} className="btn-secondary"><Printer className="h-4 w-4"/>Print / PDF</button></div></div>
  <div className="overflow-hidden rounded-2xl border bg-white"><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="font-bold">Current Stock / موجودہ اسٹاک</h2><p className="mt-1 text-xs text-slate-500">Warehouse → Godown → Item</p></div><WarehouseIcon className="h-5 w-5 text-slate-400"/></div><div className="overflow-x-auto"><table className="w-full min-w-[1050px] text-left text-sm"><thead className="border-b bg-slate-50"><tr>{["Warehouse","Godown","Item","Grade","Size","Quantity / UOM","Updated"].map(h=><th key={h} className="px-5 py-3 text-xs font-bold uppercase text-slate-500">{h}</th>)}</tr></thead><tbody className="divide-y">{loading?<tr><td colSpan={7} className="px-5 py-12 text-center">Loading stock...</td></tr>:filteredStock.length===0?<tr><td colSpan={7} className="px-5 py-12 text-center">No stock records found.</td></tr>:filteredStock.map(r=><tr key={r.id}><td className="px-5 py-4 font-semibold">{warehouseName(r.warehouse_id)}</td><td className="px-5 py-4">{godownName(r.godown_id,r.godown)}</td><td className="px-5 py-4"><div className="font-bold">{r.item?bilingual(r.item.name,r.item.name_urdu):"Unknown Item"}</div><div className="text-xs text-slate-400">{r.item?.sku??"—"}</div></td><td className="px-5 py-4">{r.item?.grade||"—"}</td><td className="px-5 py-4">{r.item?.size||"—"}</td><td className="px-5 py-4 text-right font-bold whitespace-nowrap">{qtyText(Number(r.quantity||0),r.item?.unit)}</td><td className="px-5 py-4 text-xs">{new Date(r.updated_at).toLocaleString("en-GB")}</td></tr>)}</tbody></table></div></div>
  {modalOpen&&<div className="fixed inset-0 z-[90] flex items-center justify-center bg-slate-950/60 p-4"><div className="max-h-[92vh] w-full max-w-lg overflow-y-auto rounded-2xl border bg-white"><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="font-bold">Controlled Stock Adjustment / اسٹاک ایڈجسٹمنٹ</h2><p className="mt-1 text-xs text-slate-500">Use only for verified physical-count corrections. Purchase/Sales stock must come from source documents.</p></div><button type="button" onClick={closeModal}><X className="h-5 w-5"/></button></div><form onSubmit={saveAdjustment} className="space-y-4 p-5"><div><label className="mb-1.5 block text-sm font-bold">Item / آئٹم *</label><select required value={form.item_id} onChange={e=>setForm(c=>({...c,item_id:e.target.value}))} className="w-full rounded-xl border px-3 py-2.5"><option value="">Select item</option>{items.map(i=><option key={i.id} value={i.id}>{i.sku} — {bilingual(i.name,i.name_urdu)} — UOM: {clean(i.unit)||"—"}</option>)}</select></div><div><label className="mb-1.5 block text-sm font-bold">Warehouse / ویئرہاؤس *</label><select required value={form.warehouse_id} onChange={e=>{const wid=e.target.value;const first=godowns.find(g=>g.warehouse_id===wid);setForm(c=>({...c,warehouse_id:wid,godown_id:first?.id??""}))}} className="w-full rounded-xl border px-3 py-2.5"><option value="">Select warehouse</option>{warehouses.map(w=><option key={w.id} value={w.id}>{bilingual(w.name,w.name_urdu)}</option>)}</select></div><div><label className="mb-1.5 block text-sm font-bold">Godown / گودام *</label><select required value={form.godown_id} disabled={!form.warehouse_id} onChange={e=>setForm(c=>({...c,godown_id:e.target.value}))} className="w-full rounded-xl border px-3 py-2.5"><option value="">{form.warehouse_id?"Select godown":"Select warehouse first"}</option>{modalGodowns.map(g=><option key={g.id} value={g.id}>{bilingual(g.name,g.name_urdu)}</option>)}</select></div><div><label className="mb-1.5 block text-sm font-bold">New Physical Quantity / نئی اصل مقدار *</label><div className="flex overflow-hidden rounded-xl border"><input required type="number" min="0" step="0.001" value={form.qty} onChange={e=>setForm(c=>({...c,qty:e.target.value}))} className="min-w-0 flex-1 border-0 px-3 py-2.5 outline-none"/><span className="flex min-w-[64px] items-center justify-center border-l bg-slate-50 px-3 font-bold">{uom}</span></div></div><div><label className="mb-1.5 block text-sm font-bold">Reason / Reference / وجہ و حوالہ *</label><textarea required rows={3} value={form.reference} onChange={e=>setForm(c=>({...c,reference:e.target.value}))} placeholder="Physical count difference, damage verification, opening correction approval reference, etc." className="w-full rounded-xl border px-3 py-2.5"/></div><div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900"><strong>Controlled rule:</strong> Direct manual Stock IN/OUT is disabled. This action sets the verified physical quantity and records the user, time and reason in stock movement history.</div><div className="flex justify-end gap-2 border-t pt-4"><button type="button" onClick={closeModal} className="btn-secondary"><X className="h-4 w-4"/>Cancel</button><button type="submit" disabled={saving||!form.warehouse_id||!form.godown_id} className="btn-primary"><Save className="h-4 w-4"/>{saving?"Saving...":"Save Adjustment"}</button></div></form></div></div>}
 </div>
}
