import { useEffect,useMemo,useState } from "react";import { supabase } from "@/lib/supabase";
type Plan={id:string;name:string;billing_cycle:"monthly"|"yearly"|"custom";price:number;max_users:number};type Sub={id:string;plan_id:string|null;billing_cycle:string;starts_at:string;expires_at:string|null;amount:number};type Mod={module_key:string;enabled:boolean};const MODULES=[["master","Master Data"],["sales","Sales"],["purchase","Purchase"],["inventory","Inventory / Godown"],["production","Production / Cutting"],["accounting","Accounting"],["reports","Reports"],["settings","Settings"]] as const;
export default function SubscriptionControl({companyId,onSaved}:{companyId:string;onSaved?:()=>Promise<void>|void}){const[plans,setPlans]=useState<Plan[]>([]),[sub,setSub]=useState<Sub|null>(null),[mods,setMods]=useState<Mod[]>([]),[busy,setBusy]=useState(false),[msg,setMsg]=useState("");const[planId,setPlanId]=useState(""),[billing,setBilling]=useState<"monthly"|"yearly"|"custom">("monthly"),[expiry,setExpiry]=useState(""),[amount,setAmount]=useState("0");const load=async()=>{const[p,s,m]=await Promise.all([supabase.from("subscription_plans").select("id,name,billing_cycle,price,max_users").eq("is_active",true).order("name"),supabase.from("company_subscriptions").select("id,plan_id,billing_cycle,starts_at,expires_at,amount").eq("company_id",companyId).in("status",["trial","active","past_due","suspended"]).limit(1),supabase.from("company_modules").select("module_key,enabled").eq("company_id",companyId)]);const e=p.error||s.error||m.error;if(e)throw e;setPlans((p.data??[]) as Plan[]);setSub((s.data?.[0]??null) as Sub|null);setMods((m.data??[]) as Mod[])};useEffect(()=>{void load().catch(e=>setMsg(e.message))},[companyId]);const enabled=useMemo(()=>new Set(mods.filter(x=>x.enabled).map(x=>x.module_key)),[mods]);useEffect(()=>{const p=plans.find(x=>x.id===sub?.plan_id);setPlanId(sub?.plan_id??plans[0]?.id??"");setBilling((sub?.billing_cycle as any)??p?.billing_cycle??"monthly");setAmount(String(sub?.amount??p?.price??0));setExpiry(sub?.expires_at?new Date(sub.expires_at).toISOString().slice(0,10):"")},[companyId,sub?.id,plans.length]);const run=async(fn:()=>Promise<void>)=>{setBusy(true);setMsg("");try{await fn();await load();await onSaved?.();setMsg("Saved successfully.")}catch(e){setMsg(e instanceof Error?e.message:"Request failed")}finally{setBusy(false)}};const save=()=>run(async()=>{const plan=plans.find(x=>x.id===planId),expires=expiry?`${expiry}T23:59:59.999Z`:null,payload={company_id:companyId,plan_id:planId||null,billing_cycle:billing,status:"active",starts_at:sub?.starts_at??new Date().toISOString(),expires_at:expires,amount:Number(amount)||0,updated_at:new Date().toISOString()};const q=sub?supabase.from("company_subscriptions").update(payload).eq("id",sub.id):supabase.from("company_subscriptions").insert(payload);const{error}=await q;if(error)throw error;const{error:ce}=await supabase.from("companies").update({subscription_expires_at:expires,max_users:plan?.max_users??10,status:"active",updated_at:new Date().toISOString()}).eq("id",companyId);if(ce)throw ce});const toggle=(key:string,value:boolean)=>run(async()=>{const{error}=await supabase.from("company_modules").upsert({company_id:companyId,module_key:key,enabled:value,updated_at:new Date().toISOString()},{onConflict:"company_id,module_key"});if(error)throw error});return <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><h2 className="font-semibold text-slate-900">Subscription & Module Access</h2><p className="mt-1 text-xs text-slate-500">Monthly/yearly access and company-level module switches.</p>{msg&&<div className="mt-3 rounded-lg bg-slate-50 px-3 py-2 text-xs">{msg}</div>}<div className="mt-3 grid gap-3 sm:grid-cols-4"><label className="text-xs">Plan<select className="input mt-1 w-full" value={planId} onChange={e=>{setPlanId(e.target.value);const p=plans.find(x=>x.id===e.target.value);if(p){setBilling(p.billing_cycle);setAmount(String(p.price))}}}>{plans.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="text-xs">Billing<select className="input mt-1 w-full" value={billing} onChange={e=>setBilling(e.target.value as any)}><option value="monthly">Monthly</option><option value="yearly">Yearly</option><option value="custom">Custom</option></select></label><label className="text-xs">Expiry<input className="input mt-1 w-full" type="date" value={expiry} onChange={e=>setExpiry(e.target.value)}/></label><label className="text-xs">Amount<input className="input mt-1 w-full" type="number" min="0" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)}/></label></div><button className="btn-primary mt-3" disabled={busy||!planId} onClick={()=>void save()}>Save Subscription</button><div className="mt-5 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{MODULES.map(([key,label])=><label key={key} className="flex items-center justify-between rounded-lg border border-slate-200 px-3 py-2 text-sm"><span>{label}</span><input type="checkbox" checked={enabled.has(key)} disabled={busy} onChange={e=>void toggle(key,e.target.checked)}/></label>)}</div></section>}
